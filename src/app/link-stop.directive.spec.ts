import { LinkStopDirective } from './link-stop.directive';

describe('LinkStopDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkStopDirective();
    expect(directive).toBeTruthy();
  });
});
